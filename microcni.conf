{
    "cniVersion": "0.3.1",
    "name": "microcni",
    "type": "microcni",

    # Pod CIDR of the node
    # Make the pod CIDR in this file and in the `init.sh`
    # file match on the same node.
    "podcidr": "10.240.0.0/24" 
}

